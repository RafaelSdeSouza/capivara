% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structural_awareness.R
\name{detect_ring}
\alias{detect_ring}
\title{Detect a ring-like annular structure}
\usage{
detect_ring(
  input = NULL,
  scores = NULL,
  support = NULL,
  support_method = "starlet",
  structure_scales = 3:4,
  footprint_quantile = 0.2,
  bulge_fraction = 0.25,
  min_inner_radius_fraction = 0.22,
  max_radius_fraction = 1.05,
  threshold_quantile = 0.05,
  min_area = 24L,
  min_azimuth_coverage = 0.22,
  min_confidence = 0.35,
  keep_largest_component = TRUE,
  ring_model_floor = 0.08,
  n_radial_bins = 18L,
  starlet_args = list()
)
}
\arguments{
\item{input}{A FITS-like object with \code{imDat}, or a 3-D cube array. Used
only when \code{scores} is not supplied.}

\item{scores}{Optional object from \code{\link{score_structures}}.}

\item{support}{Optional support object from \code{\link{detect_support}}.}

\item{support_method}{Support detector used when \code{scores} is not
supplied.}

\item{structure_scales}{Starlet scales used by \code{\link{score_structures}}.}

\item{footprint_quantile}{Quantile of the collapsed positive image used to
build the broad galaxy footprint before searching for annular structure.}

\item{bulge_fraction}{Elliptical-radius fraction used to exclude the compact
central component from the ring support.}

\item{min_inner_radius_fraction}{Minimum elliptical radius considered for
the ring, as a fraction of the 95-percent support radius. The default is
intentionally outside the compact bulge.}

\item{max_radius_fraction}{Maximum elliptical radius considered for the
ring, as a fraction of the 95-percent support radius.}

\item{threshold_quantile}{Quantile of the ring score used to seed the
candidate mask.}

\item{min_area}{Minimum candidate area in pixels.}

\item{min_azimuth_coverage}{Minimum azimuthal coverage, in fractions of
\eqn{2\pi}, required for \code{ring_like = TRUE}.}

\item{min_confidence}{Minimum confidence for \code{ring_like = TRUE}.}

\item{keep_largest_component}{If \code{TRUE}, keep only the largest connected
ring component after thresholding. This is useful for suppressing small
starlet islands around the main annular feature.}

\item{ring_model_floor}{Minimum value of the fitted annular model kept in
the final ring mask. Lower values keep faint/asymmetric ring arcs.}

\item{n_radial_bins}{Number of bins used in the elliptical radial profile.}

\item{starlet_args}{Extra arguments passed to \code{\link{score_structures}}
when \code{scores} is not supplied.}
}
\value{
A \code{capivara_ring_detection} object containing \code{ring_mask},
  \code{candidate_mask}, \code{ring_score}, \code{ring_model},
  \code{diagnostics}, and \code{radial_profile}. The logical field
  \code{ring_like} is deliberately conservative.
}
\description{
Experimental ring detector for structural-aware segmentation. The detector
combines the neutral Capivara structure score with a simple elliptical
annulus model: it estimates the galaxy geometry from the supported light,
builds an elliptical radial profile, finds the strongest non-central
annular peak, and thresholds only pixels close to that peak. The central
region is explicitly excluded, so a bright bulge is not labelled as a ring.
}
