% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_large.R
\name{segment_large}
\alias{segment_large}
\title{Large-cube Capivara segmentation}
\usage{
segment_large(
  input,
  Ncomp = 15,
  redshift = 0,
  scale_fn = median_scale,
  target_snr = NULL,
  var_cube = NULL,
  k_values = NULL,
  wavelength_range = NULL,
  feature_wavelength_range = NULL,
  snr_stat = c("integrated", "median_per_wavelength"),
  variance_inflation = 1,
  use_starlet_mask = FALSE,
  support_method = c("starlet", "adaptive"),
  support_args = list(),
  collapse_fn = collapse_white_light,
  starlet_J = 5,
  starlet_scales = 2:5,
  include_coarse = FALSE,
  denoise_k = 2.5,
  starlet_mode = c("soft", "hard"),
  positive_only = TRUE,
  mask_mode = c("na", "zero"),
  knn_k = 40,
  auto_k = TRUE,
  max_k = NULL,
  feature_scale = c("none", "robust_col"),
  spatial_weight = 0,
  mask = NULL,
  valid_mode = c("sagui", "signal", "finite"),
  return_details = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{input}{A FITS-like object with `imDat`, or a raw 3D array.}

\item{Ncomp}{Integer, the number of clusters to form. Defaults to `15`.}

\item{redshift}{Kept for API compatibility with `segment()`.}

\item{scale_fn}{Optional row-wise spectral scaling function. Defaults to
\code{\link{median_scale}}, matching \code{\link{segment}}.}

\item{target_snr}{Optional minimum accepted SNR per cluster. When supplied,
Capivara chooses the largest number of clusters whose minimum cluster SNR
remains above this threshold.}

\item{var_cube}{Optional variance cube matching the input cube. Used only
when \code{target_snr} is supplied.}

\item{k_values}{Optional candidate cluster counts tested when
\code{target_snr} is supplied. For this scalable backend, the default grid
is capped at 50 clusters.}

\item{wavelength_range}{Optional wavelength interval used to compute SNR when
\code{target_snr} is supplied.}

\item{feature_wavelength_range}{Optional wavelength interval used to select
the spectral channels used for clustering. The returned
\code{original_cube} remains the full input cube so downstream summed
spectra are still flux-preserving across the full spectral axis.}

\item{snr_stat}{Either integrated SNR or median per-wavelength SNR when
\code{target_snr} is supplied.}

\item{variance_inflation}{Multiplicative factor applied to propagated
variances when \code{target_snr} is supplied.}

\item{use_starlet_mask}{Logical; if \code{TRUE}, build a Sagui-style
support mask before clustering.}

\item{support_method}{Foreground support builder used when
\code{use_starlet_mask = TRUE}. Options are \code{"starlet"} and
\code{"adaptive"}.}

\item{support_args}{Optional named list passed to the selected support
builder. For \code{"adaptive"}, arguments are passed to
\code{\link{build_adaptive_support}}.}

\item{collapse_fn}{Function used to collapse the cube to white light when
\code{use_starlet_mask = TRUE} and \code{support_method = "starlet"}.}

\item{starlet_J}{Number of starlet scales when \code{use_starlet_mask = TRUE}.}

\item{starlet_scales}{Integer vector of scales kept in the reconstruction
when \code{use_starlet_mask = TRUE}.}

\item{include_coarse}{Logical; include the coarse starlet plane when
\code{use_starlet_mask = TRUE}.}

\item{denoise_k}{Optional denoising threshold in MAD units when
\code{use_starlet_mask = TRUE}.}

\item{starlet_mode}{Thresholding mode for the starlet reconstruction when
\code{use_starlet_mask = TRUE}.}

\item{positive_only}{Logical; keep only positive reconstructed values when
\code{use_starlet_mask = TRUE}.}

\item{mask_mode}{Either \code{"na"} or \code{"zero"} for masked spaxels
when \code{use_starlet_mask = TRUE}.}

\item{knn_k}{Number of nearest neighbours for the sparse Ward graph.}

\item{auto_k}{If TRUE, increase `knn_k` when the graph is too disconnected.}

\item{max_k}{Maximum k allowed when `auto_k = TRUE`.}

\item{feature_scale}{Optional column-wise feature scaling after row scaling.}

\item{spatial_weight}{Optional weight for appending normalized x/y coordinates.}

\item{mask}{Optional logical spatial mask with dimensions n_row x n_col.}

\item{valid_mode}{Valid-pixel rule. \code{"sagui"} applies the stricter
finite-fraction, energy, and row-MAD screen used by Sagui's sparse-Ward
backend; \code{"signal"} matches the exact \code{\link{segment}} support;
\code{"finite"} requires all channels to be finite.}

\item{return_details}{Return features, labels, and diagnostics.}

\item{verbose}{Print progress messages.}
}
\value{
A Capivara-style segmentation object with the same core fields as
  \code{\link{segment}}: \code{cluster_map}, \code{header}, \code{axDat},
  \code{cluster_snr}, and \code{original_cube}. Scalable-backend diagnostics
  are stored under \code{backend_info}.
}
\description{
This backend keeps Ward's merge criterion but restricts candidate merges to a
k-nearest-neighbor graph. It is intended for large cubes where
\code{\link{segment}} becomes memory-limited because exact Ward needs an
all-pairs distance object.
}
