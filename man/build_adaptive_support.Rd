% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive_support.R
\name{build_adaptive_support}
\alias{build_adaptive_support}
\title{Build an adaptive multi-band/spatial support mask}
\usage{
build_adaptive_support(
  input,
  bands = NULL,
  transform = c("none", "asinh", "signed_log1p", "log1p", "copula_uniform",
    "copula_gaussian"),
  sky_method = c("border", "all"),
  border_fraction = 0.1,
  z_threshold = 3,
  min_band_persistence = NULL,
  single_band_z = Inf,
  smooth_sigma = 0
)
}
\arguments{
\item{input}{A FITS-like object with \code{imDat}, or a 3-D cube array.}

\item{bands}{Wavelength slices used to build the support. May be \code{NULL}
for all slices, a numeric vector of indices, or a character vector matching
cube dimnames along the third dimension.}

\item{transform}{Per-slice image transform before robust sky normalization.
Options are \code{"none"}, \code{"asinh"}, \code{"signed_log1p"}, \code{"log1p"},
\code{"copula_uniform"}, and \code{"copula_gaussian"}.}

\item{sky_method}{Pixels used for robust sky/noise estimation. \code{"border"}
uses image borders; \code{"all"} uses all finite pixels.}

\item{border_fraction}{Fraction of rows/columns used as image border when
\code{sky_method = "border"}.}

\item{z_threshold}{Per-slice detection threshold in robust sigma units.}

\item{min_band_persistence}{Minimum number of selected slices above
\code{z_threshold}. If \code{NULL}, defaults to two slices when possible.}

\item{single_band_z}{Optional single-slice rescue threshold. Use \code{Inf} to
disable.}

\item{smooth_sigma}{Optional Gaussian smoothing applied to each significance
map before thresholding. Requires \code{EBImage} or \code{imager}.}
}
\value{
A list with \code{collapsed}, \code{reconstruction}, \code{mask},
\code{evidence}, \code{evidence_count}, \code{z_maps}, \code{band_stats}, and
metadata.
}
\description{
Builds a foreground support from band-wise or wavelength-slice detection
evidence rather than from a single white-light image. This is useful for IFU
cubes where the source is persistent over many wavelength channels but the
background may vary spatially or spectrally.
}
