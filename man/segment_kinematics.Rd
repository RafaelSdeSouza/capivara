% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinematics_run_manga_bar_model.R
\name{segment_kinematics}
\alias{segment_kinematics}
\title{Segment an IFU cube using emission-line kinematics}
\usage{
segment_kinematics(
  cube_path,
  redshift = NA_real_,
  emission_line = "halpha",
  segmentation_mode = c("kinematic", "path_signature"),
  output_dir = NULL,
  object_id = NULL,
  knn_k = 50,
  n_segments = 25,
  n_path_segments = 45,
  starlet_scales = "2:5",
  include_coarse_starlet = FALSE,
  support_mode = c("starlet", "line_flux"),
  line_flux_sigma = 3,
  show_plots = interactive()
)
}
\arguments{
\item{cube_path}{Path to a MaNGA LOGCUBE.}

\item{redshift}{Optional redshift. If `NA`, uses FITS header then local
MaNGA metadata.}

\item{emission_line}{Emission line alias, such as `"halpha"` or
`"oiii5007"`.}

\item{segmentation_mode}{`"kinematic"` clusters flux, velocity, dispersion,
and profile-shape maps. `"path_signature"` additionally computes the
path-signature feature segmentation.}

\item{output_dir}{Directory for saved products. Defaults beside the cube.}

\item{object_id}{Optional output identifier.}

\item{knn_k}{kNN graph size for sparse Ward clustering.}

\item{n_segments}{Number of kinematic-aware segments.}

\item{n_path_segments}{Number of path-signature segments.}

\item{starlet_scales}{Starlet support scales.}

\item{include_coarse_starlet}{Include the coarse starlet plane in support.}

\item{support_mode}{Foreground support for the kinematic maps. `"starlet"`
keeps the white-light starlet footprint; `"line_flux"` trims that
footprint with a robust emission-line-flux threshold before fitting.}

\item{line_flux_sigma}{Border-noise threshold, in robust sigma units, when
`support_mode = "line_flux"`.}

\item{show_plots}{Print the compact kinematic panel.}
}
\value{
A `capivara_kinematic_segmentation` object containing native maps,
  support, kinematic segmentation, optional path segmentation, and paths.
}
\description{
Builds native emission-line maps, then clusters either the kinematic feature
cube or the path-signature feature cube. It deliberately does not run the
ordinary full-spectrum Capivara segmentation: use [segment()] or
[segment_large()] when spectral segmentation is the scientific objective.
}
