% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disc_model.R
\name{fit_bisymmetric_model}
\alias{fit_bisymmetric_model}
\title{Fit the projected bisymmetric velocity model used by the bar paper}
\usage{
fit_bisymmetric_model(
  spaxels,
  geometry,
  bar_geometry,
  n_rings = 10L,
  use_errors = TRUE,
  smooth_lambda = 10,
  second_order_lambda = 500,
  fixed_vsys = NULL,
  robust = FALSE,
  robust_k = 1.345,
  robust_maxit = 20L,
  max_v2_fraction = 0.6,
  max_mean_v2 = 350
)
}
\arguments{
\item{spaxels}{Data frame with valid spaxel coordinates, velocities,
uncertainties, deprojected radius/theta, and segmentation class.}

\item{geometry}{Disc geometry list from `estimate_disc_geometry()`.}

\item{bar_geometry}{Bar geometry list from `estimate_bar_geometry()`.}

\item{n_rings}{Number of radial knots for the piecewise velocity profiles.}

\item{use_errors}{If `TRUE`, weight by velocity errors when available.}

\item{smooth_lambda}{First-order smoothing penalty for radial profiles.}

\item{second_order_lambda}{Curvature penalty for second-order bar terms.}

\item{fixed_vsys}{Optional fixed systemic velocity.}

\item{robust}{If `TRUE`, iteratively downweight large residuals.}

\item{robust_k}{Huber tuning constant used by the robust fit.}

\item{robust_maxit}{Maximum robust reweighting iterations.}

\item{max_v2_fraction}{Maximum allowed mean second-order amplitude relative
to the circular term before shrinkage.}

\item{max_mean_v2}{Absolute cap on mean second-order velocity amplitude.}
}
\value{
A list with fitted spaxels, radial profiles, parameters, and
  diagnostics.
}
\description{
This implements the Section 3 line-of-sight model without PSF convolution:
Vlos = Vsys + sin(i) * [Vt cos(theta)
  - V2t cos(2(theta - phi_b)) cos(theta)
  - V2r sin(2(theta - phi_b)) sin(theta)].
}
